# AHRIP - Build Progress

**Source of truth:** `FEATURES.md` (audited inventory, single file).
**Started:** 2026-05-01
**Last audited:** 2026-05-04
**Stack:** Flask 3 / Python 3.13 / SQLite (dev) | PostgreSQL-ready · React 18 + Vite + TS + Tailwind v3.4 · scikit-learn · Gemini 2.5 Flash + local Ollama `gemma4:e4b`.

---

## Current verification snapshot (2026-05-04)

- **Backend**: `pytest -q` → **279 passed, 1 known-broken** (`test_register_returns_201_with_job_role_and_industry`; tracked in `THINGS_TO_FIX.md`).
- **Session 2 (Manual question catalogue) landed**: 100 hand-crafted scenarios in `backend/seed_catalogue.py` spanning all 8 categories × 3 difficulties × 3 question types (87 MCQ, 7 true/false, 6 identify-threat). Idempotent CLI loader (`python seed_catalogue.py [--dry-run]`), validates every option via `validate_options` before any DB write. 11 regression tests in `test_seed_catalogue.py`.
- **Session 3 (Adaptive Engine v2) landed**: Dim 3 real-time threat slot, Dim 5 sentiment-driven difficulty shift, Dim 6 cognitive-load / fatigue guard, Dim 9 risk-banded spaced-repetition window. Covered by `backend/tests/test_adaptive_engine_v2.py` (17 tests).
- **Session 4 (Behavioural profiler) landed**: Dim 7 mistake clustering, Dim 8 peer-relative difficulty (wired into `select_next_session` as `peer_shift` combined with sentiment), Dim 10 motivation profiling (`dabbler` / `streak_keeper` / `leaderboard_chaser` / `badge_hunter` / `completionist` / `balanced`). New service `backend/app/services/behavioral_profiler.py`; API `GET /api/v1/training/insights`; 17 new tests in `test_behavioral_profiler.py`.
- **Session-start API** now returns `selection_meta` (sentiment shift, peer shift, combined shift, threat slot used, spacing window) alongside scenarios; `/answer` response includes `fatigue.{fatigue_signal, recommended_pause, reason}`.
- **Session size**: reduced from 20 → `SESSION_SIZE = 5` (cognitive-load guard); distribution rebalanced to 60/20/20 so 5-slot sessions keep a real "other / challenge" mix.
- **Frontend**: `tsc --noEmit` clean · `vitest run` → **11/11** passed · `vite build` ok (42 KB CSS + 1.14 MB JS, gzip ≈ 315 KB).
- **Models**: 17 SQLAlchemy models (UUID PKs) across `backend/app/models/`.
- **Migrations**: 6 alembic revisions in `backend/migrations/versions/`.
- **API blueprints**: 10 (`auth`, `training`, `scores`, `manager`, `admin`, `gamification`, `health`, `cyber_news`, `breach_chronicle`, `notifications`).
- **Background jobs**: 7 (threat ingestion, risk recalc, RF retrain, KMeans recluster, weekly XP reset, cyber news, LLM scenario generation).
- **LLM**: Gemini 2.5 Flash + local Ollama `gemma4:e4b` (`think=false`). **No paid providers.**

---

## Phase Status Legend
- ⬜ Not started
- 🟡 In progress
- ✅ Verified & committed
- ⚠️ Blocked (see THINGS_TO_FIX.md)

---

## Phases

| # | Phase | Status | Notes |
|---|-------|--------|-------|
| 0.1 | Project Scaffolding | ✅ | `create_app()` OK · `npm install` OK · `tsc --noEmit` clean. Versions bumped for Python 3.13 (sklearn / pandas / numpy / psycopg2 / SQLAlchemy). |
| 0.2 | Database Schema | ✅ | Started at 8 models; **17 models** today including `Notification`, `ProfileChangeRequest`, `AuditLog`, `CyberNews`, `FamousBreach`, `UserBehaviorEvent`, `Invite`, `TokenBlocklist`, `SystemCounter`. |
| 1   | Auth + JWT + Roles | ✅ | Username **or** email login, JWT 15 min access + 30 d refresh + blocklist, 5-attempt lockout (423), bcrypt 12 rounds, bleach on inputs. |
| 2   | OSINT Threat Pipeline | ✅ | 7-stage pipeline · Phishing.Database (replaces dead PhishTank) + Phishing.Database + OTX + URLScan with free-tier resilience · raw URL never leaks · 20-scenario per-run cap · 48 h dedupe. |
| 3   | Scenario Engine | ✅ | 48 hand-written + 6 free-text demo scenarios; 8 categories × 3 difficulties; sandboxed `visual_html`; idempotent seed. |
| 4   | Adaptive Engine + Sentiment | ✅ | Mastery λ = 0.85 · 80/40 promote/demote · 5 sentiment labels via VADER + heuristics · streak ×1.5/×2.0/×2.5/×3.0 · respects coaching hints. |
| 5   | Random Forest | ✅ | 14-feature vector · graceful fallback · `MIN_TRAINING_SAMPLES=20`. |
| 6   | K-Means Clustering | ✅ | 5 archetypes (colour + icon + intervention) · 6-feature vector · scaler + clusterer in single joblib bundle. |
| 7   | Gamification | ✅ | 12 reward keys · 12 badges × 5 rarities · UTC-stable streak dates · anonymised "First L." leaderboard. |
| 8   | Core API | ✅ | 10 blueprints at `/api/v1/*` · global JSON error handler · session-ownership enforced · cross-org returns 404. |
| 9   | Design System | ✅ | CSS-variable tokens (light theme WCAG 2.1 AA) · 5 UI primitives · `EmptyState` + `Toaster` + `NotificationBell` · `prefers-reduced-motion` honoured globally. |
| 10  | Employee Training UI | ✅ | 3-state flow + `LevelUpModal` + 6-tier streak counter + free-text input + evaluation feedback + mid-session `localStorage` persistence. |
| 11  | Manager Dashboard | ✅ | 12-col grid (KPIs + Heatmap + AreaChart + TopRisk + ClusterPie + CategoryBars + ThreatFeedBadge) + Team / Clusters / Reports pages + AssignTraining modal. |
| 12  | Admin Panel | ✅ | 7 admin pages: Dashboard, Users (+ coaching dialog), Scenarios, Threat Feeds, AI Generation, Invites, **Profile-change-requests inbox**. |
| 13  | Security Hardening | ✅ | Talisman prod-only · CORS allowlist · `sanitize_visual_html` · access token memory-only · 8-point security checklist tested. |
| 14  | Testing Suite | ✅ | Backend 202/203 · frontend Vitest 11/11 · `tsc --noEmit` clean. |
| 15  | Hosting / Deployment | ⬜ | **Deferred** - local-only for now. See REMAINING_WORK.md §A for the production-readiness gap list (Redis, secrets, SMTP, MFA, GDPR, CI/Docker, observability). |
| 16  | Invite System | ✅ | 7-day token validity, state machine, admin UI (list / create / revoke / copy URL). |
| 17  | Breach Chronicle | ✅ | Curated Hall of Shame + RSS-driven cyber news. |
| 18  | Behavioural Telemetry | ✅ | `UserBehaviorEvent` model + `telemetry_service.py`. |
| 21  | Free-text Questions + Local-LLM Grading | ✅ | 3 new question types (`short_answer`, `descriptive`, `fill_in_blank`) · 3-tier grader · Gemini deliberately never called for grading. |
| 22  | Event-driven Notifications | ✅ | 7 types × 4 severities · `NotificationBell` + Toaster · 60 s polling · auto-emitted on level-up / badge / streak / training assigned / change-request review. |
| 23  | Username Login + Coaching + Profile-change-requests | ✅ | Phase-1 (username + coaching) and Phase-2 (profile-change-request workflow) both shipped. Sidebar shows live pending-count badge. |
| B   | BONUS - Evaluation Data Collection | ⬜ | Optional. |

---

## Current Focus
**Active workstream:** Multi-track UX & reliability pass (May 2026). Priority order:
1. Session persistence (training mid-session refresh) - ✅ done in Session 1.
2. Gemini parsing + fallback reliability - ✅ done in Session 1.
3. Training-system upgrade (free-text questions + Ollama answer evaluation) - ✅ done in Session 2.
4. Provider status (live Ollama probe) - ✅ done in Session 1.
5. Notifications system (event-driven) - ✅ done in Session 3.
6. Light-theme contrast audit - ✅ done in Session 4.
7. General UI polish - ✅ done in Session 4 (focus ring, reduced-motion, EmptyState, Toaster).

Hosting/deployment (formerly "Phase 15") is **deferred** until the local-only experience is feature-complete.

## Recent Activity Log
- 2026-05-04  🧹 **Training-quality Session 1 - data hygiene.** Root-caused the "Answer……" / "Option C" stub bug to `answer_utils._pad_to`: when the LLM emitted a 3-7 char stub like `"Answer"`, the padder appended random filler (`"Answer, after a quick think, to keep the workflow moving."`) producing distractors that read as truncated. Fix: `_pad_to` now refuses to pad inputs below `MIN_MEANINGFUL_OPTION_CHARS` (12). Added `is_option_stub()` + `validate_options()` with per-question-type rules (MCQ ≥ 12 chars, identify_threat ≥ 4 chars, true_false ≥ 1 char accepts canonical "True"/"False", free-text skipped). Wired the validator into `POST /admin/scenarios` (returns 400 with `stub_options` array) and into `llm_orchestrator.generate_one` (drops bad payloads + falls through to next provider). New `backend/clean_malformed_scenarios.py` - scans every scenario, writes JSON audit report to `backend/logs/`, supports `--dry-run` / `--delete` / `--reactivate`. First run found **3 genuine data bugs** (1 historic padder corruption + 2 TF rows with False stored in slot C/D); all soft-disabled. Tests: +16 cases in `test_answer_utils.py` + `test_admin.py`; backend now **234 passed / 1 known-broken**.
- 2026-05-03  📋 **Tracking-doc audit pass.** User asked for 100 % accurate `FEATURES.md`. Rewrote FEATURES.md from scratch against the live source tree (17 models confirmed; 10 blueprints; 7 scheduled jobs; 6 alembic revisions; 6 question types; profile-change-request workflow; coaching hints; notifications + Toaster + EmptyState; new admin "Change Requests" page). Refreshed `REMAINING_WORK.md` (split into Production Readiness §A, Backend Gaps §B, Frontend Gaps §C, Cleanup §D, Bonus §E plus a "Recently Completed" section) and `THINGS_TO_FIX.md` (added the one known-broken auth test, reorganised by severity, moved 2026-05-03 deliveries to "Resolved"). PROGRESS.md updated with current verification snapshot + new Phase 21/22/23 rows. Live verification: backend 202/203 pytest · frontend `tsc --noEmit` clean · `vite build` ok.
- 2026-05-03  🔁 **Session 5: Profile-change-request workflow (Phase-23 Phase-2).** Backend tests for change-request flow + Thompson-sampler hint integration (UUID-cast bug fixes). Frontend ProfilePage gained per-field Request-change buttons + status chip + "My change requests" history card; new `<ChangeRequestDialog>` modal. New admin route `/app/admin/profile-change-requests` with status-filter tabs, table with Approve / Reject buttons, reviewer-note dialog. Sidebar entry "Change Requests" (Inbox icon) shows live pending-count badge polled every 60 s. New API methods: `authApi.submitProfileChangeRequest` / `listMyProfileChangeRequests`; `adminApi.listProfileChangeRequests` / `approveProfileChangeRequest` / `rejectProfileChangeRequest`.
- 2026-05-03  🎨 **Session 4: Light-theme WCAG pass + UI polish primitives.**
  - **Contrast audit** - light-theme CSS variables in `@/frontend/src/index.css` rebalanced to hit WCAG 2.1 AA body-text contrast (≥ 4.5:1) against the light base. Verified ratios inline in the file; the big offenders that previously failed AA and are now fixed:
    - `--text-muted` `#6B7E99 → #4E6380` (3.2 → 4.7)
    - `--accent` `#3B82F6 → #1D4ED8` (3.3 → 7.0)
    - `--risk-medium` `#EAB308 → #A16207` (1.9 → 4.6)
    - `--risk-low` `#22C55E → #15803D` (2.4 → 4.9)
    - `--risk-high` `#F97316 → #C2410C` (2.9 → 5.0)
    - `--warning` / `--xp-gold` `#F59E0B → #B45309` (2.4 → 5.4)
    - `--success` `#10B981 → #047857` (2.9 → 5.9)
    - Rarity pips + xp-silver/bronze + error also rethemed to 600/700 Tailwind stops so epic/legendary cards stay readable on white.
  - **a11y / polish**:
    - Global `:focus-visible` ring (2 px accent outline, keyboard-only) inside `@layer base` - previously the only focus indicator was the browser default, which the design-token bg eclipsed.
    - Global `prefers-reduced-motion: reduce` guard kills infinite animations (streak flame, shimmer, pulse-risk) for users with motion sensitivity while letting per-component Framer Motion opt-outs take precedence.
  - **Shared primitives** (new):
    - `@/frontend/src/components/shared/EmptyState.tsx` - reusable icon + title + optional description + optional CTA panel with a `compact` mode for nested dropdowns. Already dropped into `<NotificationBell>`'s loading / all-caught-up states and ready for team/history/leaderboard pages.
    - `@/frontend/src/components/shared/Toaster.tsx` - zero-dep toast bus (tiny zustand store + Framer Motion enter/exit). 5-toast stack, auto-dismiss at 4.5 s (6.5 s for errors), de-duped on id, sticky if `duration=0`. Exposes an imperative `toast.info/success/warning/error(title, body?)` callable from anywhere including non-React modules.
    - Mounted once in `App.tsx`; every route gets toast delivery for free.
  - **Notifications → toasts wiring**: `useNotifications` now fires a toast for every *new* unread notification that arrives while the tab is open (severity → tone map, ids tracked in a ref so the 60 s re-poll of the same item never re-toasts). First hydration is treated as a backlog and silently seeds the de-dupe set - we don't want to toast 12 day-old level-ups on page load.
  - Verification: `tsc --noEmit` clean · backend **182/182 pytest** still green.
  - To use: `toast.success("Model trained")` from any page; `<EmptyState icon={Users} title="No team members yet" description="Invite someone from the Users page." />` from any list.
- 2026-05-03  🔔 **Session 3: Event-driven notifications system.**
  - **Schema** (alembic revision `b1e22f407ac9_phase22_notifications`): new `notifications` table - `id`, `user_id (FK cascade)`, `organisation_id (FK cascade)`, `type`, `severity`, `title`, `body`, `link`, `meta_json`, `read_at`, `created_at`, `updated_at`. Indexed on `user_id`, `organisation_id`, `type`, `read_at`.
  - **`app/models/notification.py`** (new) - 7 notification types (`level_up`, `badge_earned`, `training_assigned`, `risk_escalation`, `streak_milestone`, `announcement`, `system`) × 4 severities (`info`/`success`/`warning`/`critical`). `meta` property transparently JSON-serialises a dict for arbitrary payloads; `mark_read()` is idempotent.
  - **`app/services/notifications.py`** (new) - defensive `emit()` helper (coerces stringy UUIDs, rolls back on failure so the primary user action can never break), plus 6 typed helpers: `emit_level_up`, `emit_badge_earned`, `emit_streak_milestone`, `emit_training_assigned` (summarises categories + truncates note at 120 chars), `emit_risk_escalation`, `emit_announcement`, and `broadcast_to_org` for fan-out to every active user. `STREAK_MILESTONES = {3, 7, 14, 30, 60, 100}` - only emitted when the streak **crosses** a tier, never on the n+1st day at the same tier.
  - **`app/api/notifications.py`** (new blueprint at `/api/v1/notifications`): `GET /` (paginated, newest-first, `?unread=1` filter), `GET /unread-count` (cheap polling endpoint), `PATCH /<id>/read`, `POST /read-all`, `DELETE /<id>`. Every endpoint is user-scoped - another user's notification returns 404.
  - **Emission wiring**:
    - `adaptive_engine.process_attempt` - `_update_gamification` now also returns `streak_crossed_milestone`. Emits level-up on `new_level`, one per earned badge (with `BADGES[id].name` + `rarity` in meta), and streak milestones. All emitted inside the existing transaction so they land atomically with the attempt row.
    - `manager.assign_training` - emits `training_assigned` to the target employee with the manager's name, category list, and optional note. Endpoint still returns 202 (persistence of the assignment itself is still deferred per THINGS_TO_FIX).
  - **Frontend**:
    - `types/api.ts` - `NotificationType` / `NotificationSeverity` / `NotificationDto` / `NotificationListResponse`.
    - `api/notifications.ts` - typed client (`list` / `unreadCount` / `markRead` / `markAllRead` / `dismiss`).
    - `hooks/useNotifications.ts` - gated on `accessToken`, initial fetch on mount, polls `unread-count` every **60 s** (cheap), refreshes the full list whenever the dropdown opens. All mutations optimistic; next poll reconciles any divergence.
    - New `<NotificationBell>` in `components/layout/` - bell icon with red count badge (capped at `99+`), 360 px dropdown that closes on outside click, per-type Lucide icons (`Sparkles` for level-up, `Award` for badge, `BookOpen` for training assigned, `TriangleAlert` for risk, `Flame` for streak, `Megaphone` for announcement), severity-tinted icon chip, 2-line clamped body, relative timestamp (`s/m/h/d ago` → locale date), unread dot + subtle accent tint, per-row `×` dismiss on hover, "Mark all read" header action. Clicking a notification navigates to its `link` and marks it read.
    - `TopBar.tsx` - replaced the static `<button><Bell/></button>` placeholder with `<NotificationBell />`.
  - **Tests**: new `tests/test_notifications.py` - 14 cases: emit creates row + org propagation, unknown type coerced to `system`, typed helpers set meta/severity, `training_assigned` body summarises categories + quotes note, `broadcast_to_org` skips inactive users, list/newest-first, `unread-count`, `mark-read` flips + rejects cross-user (404), `read-all` zeros unread, `dismiss` hard-deletes, auth required, `process_attempt` emits `level_up` when crossing L1→L2 (XP=110), manager `assign-training` emits `training_assigned` with correct body. **Backend now 182/182 pytest · frontend `tsc --noEmit` clean.**
  - To use: run `flask db upgrade` in `backend/`, then trigger any level-up / badge / assignment - the bell in the top-right surfaces them in near-real-time (60 s worst case).
- 2026-05-03  🧠 **Session 2: Training-system upgrade - free-text questions with local-LLM grading.**
  - **Schema** (`scenarios` + `attempts`, alembic revision `39c6a102ab87_phase21_free_text_questions`):
    - `scenarios`: `model_answer`, `key_points` (JSON), `accepted_answers` (pipe-separated), `min_words`, `max_words` - all nullable.
    - `attempts`: `answer_text`, `evaluation_score`, `evaluation_label`, `evaluation_feedback` - all nullable.
    - `Scenario.question_type` taxonomy expanded from 3 → 6: + `short_answer`, `descriptive`, `fill_in_blank`. Added `QUESTION_TYPES` and `FREE_TEXT_QUESTION_TYPES` constants for a single source of truth.
  - **`app/services/answer_evaluator.py`** (new, 350 LOC) - 3-tier grader:
    1. `fill_in_blank` exact match (case-/whitespace-insensitive, no LLM call).
    2. Ollama rubric grading via `gemma4:e4b` + `think=false` + `format=json`. System prompt forces strict `{score, label, feedback, suggestion, key_points_hit, key_points_missed}` JSON; brace-balanced JSON extractor handles markdown fences / prose wrappers. We **never** call Gemini here (would burn quota fast - every active session has up to N free-text grading calls).
    3. Deterministic heuristic fallback when Ollama is unreachable: 70 % key-point keyword coverage + 20 % length window + 10 % substance check. Caps at 0.85 and labels `uncertain` so we never claim "correct" without LLM say-so.
  - **`process_attempt`** branches on `scenario.question_type`. Free-text path: stores `answer_text` + grader fields, sets `is_correct = score ≥ 0.6` so mastery / RF features / XP / leaderboard work unchanged. Synthetic `answer_given = "~"` keeps the existing NOT-NULL invariant happy without polluting MCQ analytics.
  - **Training API**: `POST /training/session/<id>/answer` now accepts either `answer` (letter) or `answer_text` (string ≤ 4000 chars). Response payload includes `question_type`, plus an `evaluation` block + `model_answer` reveal when free-text.
  - **Admin scenario create** (`POST /admin/scenarios`): branches required-field validation, accepts `model_answer`, `key_points` (list or JSON string), `accepted_answers` (list or pipe-string), `min_words`, `max_words`. Defaults `option_*` to em-dash placeholders for free-text so the NOT-NULL columns stay valid.
  - **Frontend**:
    - `types/api.ts`: extended `QuestionType` union, exported `FREE_TEXT_QUESTION_TYPES` + `isFreeText()` guard, added `AnswerEvaluation` shape, surfaced `min_words` / `max_words` on `ScenarioPublic`.
    - `api/training.ts`: new `submitFreeTextAnswer` with **90-second timeout** (cold Ollama can take 15-60 s) and `AnswerResponse` extended with optional `evaluation` + `model_answer`.
    - `hooks/useTraining.ts`: new `submitFreeText` action with friendly error copy when daemon is down.
    - New `<FreeTextInput>` component - 1-line input / 4-row textarea / 8-row textarea per `question_type`, live word counter (amber when below `min_words`, red when over `max_words`), submit-disabled-while-blank, autoFocus, "Graded locally by Ollama" sublabel.
    - New `<EvaluationFeedback>` component - score chip + label tone, two-column rubric (hit ✓ / missed ✗), suggestion callout, collapsible expert-reference reveal, provider-tag footer (`Ollama (local)` / `Exact match` / `Offline heuristic`).
    - `TrainingPage` branches active rendering: free-text scenarios get `<FreeTextInput>` then `<EvaluationFeedback>` + `<FeedbackPanel>`; MCQ-style is unchanged.
  - **Seed**: `backend/seed_free_text.py` populates 6 demo scenarios (2 `short_answer`, 2 `descriptive`, 2 `fill_in_blank`) across phishing_email / usb_baiting / vishing / password_hygiene / social_engineering / smishing. Idempotent.
  - **Tests**: new `tests/test_answer_evaluator.py` - 14 cases covering fill-in-blank exact match (4), heuristic fallback (3), Ollama HTTP path (mocked, 2), `process_attempt` integration (2), training-API round-trip + 400-validation + MCQ regression (3). **Backend now 168/168 pytest, frontend `tsc --noEmit` clean.**
  - To use: ensure Ollama is up with `gemma4:e4b` pulled, run `python seed_free_text.py` from `backend/`, then start a training session - free-text scenarios mix in alongside MCQs since the adaptive engine treats them as ordinary scenarios.
- 2026-05-03  🛠️ **Session 1 of multi-track UX/reliability pass.**
  - **FEATURES.md**: stripped all "production / Render / Vercel / Supabase" deployment language since project is local-only for now (Phase 15 marked deferred).
  - **Gemini parser hardened**: switched to `response_mime_type='application/json'` (forces native JSON output), replaced the greedy `re.search(r"\{.*\}", DOTALL)` with an O(n) brace-depth walker that handles prose-wrapped output and stray `}` in narrative, and added a 500-char preview log line whenever parsing fails so the operator can see *why* (markdown? safety block? truncated? prose?). Fallback chain (Gemini → Ollama) was already correct in `llm_orchestrator.py`.
  - **New endpoint** `GET /api/v1/admin/ollama/health` - cheap Ollama-only liveness + model-pulled probe. Polled every 30 s from `AIGenerationPage` so the operator sees daemon flips in near-real-time. **We deliberately never poll Gemini** to preserve free-tier quota.
  - **Provider Status panel** rebuilt: Gemini tile now reads `Cloud · limited usage`; Ollama tile reads `Local · unlimited · offline capable`, shows live "live" pulse dot tinted by reachability, surfaces the actual model from the live probe (not config), and warns if daemon is up but the configured model isn't pulled (e.g. `gemma4:e4b` not yet pulled).
  - **Training session persistence** (priority 1): `useTraining` now checkpoints state to `localStorage` (`ahrip.training.session`, version-keyed) on every state change and rehydrates synchronously on mount. Refreshing or navigating away mid-session resumes on the same scenario index with the same answers - backend already keys all writes by `session_id` and re-validates ownership, so client-side restore is safe.
  - **Streak counter**: replaced 2-tier (off/hot/legendary) glow logic with **6-tier progression** (day 0 ember → day 1 amber → day 3 orange → day 7 hot orange → day 14 red → day 30 legendary gold). Glow radius + saturation + flicker speed all scale smoothly so the chip is alive from day 1 instead of staying gray until day 7.
  - Verification: backend `python -m py_compile` clean on `gemini_generator.py / ollama_fallback.py / admin.py`; frontend `tsc --noEmit` clean.
  - **Deferred to next sessions**: Task 5 (notifications system), Task 6 (light-theme contrast audit), Task 7 (training-system upgrade with free-text questions + Ollama-driven answer evaluation - major schema + service work), Task 9 (general UI polish).
- 2026-05-03  ⚡ **Local LLM swap: Ollama default changed from `llama3.1` to `gemma4:e4b` with `think=false`.**
  - Added `"think": false` to the `/api/generate` payload in `ollama_fallback.py` so reasoning-capable variants skip the chain-of-thought preamble (roughly halves latency; no-op on non-thinking models).
  - Updated defaults in `config.py`, `ollama_fallback.py`, `backend/.env`, `backend/.env.example`, `admin.py` (`ai-generation/status` response), `llm_orchestrator.py` docstring + provider aliases (added `gemma`, `gemma4`, `gemma4:e4b` aliases; kept legacy `llama` + `llama3.1` aliases for backwards compat).
  - Frontend: `AIGenerationPage.tsx` tagline and `api/admin.ts` comment now read `Gemini 2.5 Flash · Ollama gemma4:e4b (think=false)`.
  - `FEATURES.md` single-file feature inventory written at repo root; references to other tracking MDs removed from it per user directive.
- 2026-05-02  🧹 **Cleanup pass per user request - paid LLMs purged, dead OSINT sources fixed, AI generation page rebuilt.**
  - LLM stack: now strictly **Gemini 2.5 Flash + Ollama llama3.1**. Removed last `kimi` reference (`models/scenario.py:75`) and last `nvidia_enabled` field (`frontend/src/api/admin.ts`). `AIGenerationStatus` type now exposes `gemini_model`, `ollama_model`, `ollama_reachable`.
  - PhishStats CSV has been 404 since early 2026 - defaulted `PHISHSTATS_FEED_URL` (in `.env`, `.env.example`, `config.py`) to the actively-maintained **Phishing.Database** GitHub feed (no API key, daily refresh).
  - OTX free-tier resilience: `_fetch_otx` now tries `/pulses/subscribed` *and* `/pulses/activity` (deduped), with broader keyword filtering (`lure`, `spoof`, `imperson` added).
  - URLScan free-tier resilience: `_fetch_urlscan` now walks a 3-query fallback chain (`task.tags:"phishing"` → `verdicts.overall.malicious:true` → `page.url:login/verify/account`), skipping past 401/403 to find one query the key is allowed to run.
  - `fix_answer_patterns.py` rewritten - now applies `answer_utils.normalise_options` (length-balance + rolling-window letter shuffle) to **every** scenario (not just `source=manual`), with `--source` filter + `--dry-run` flag. Threat-feed and AI-generated scenarios already normalise at creation time, so this only matters for legacy/seeded rows.
  - Threat feed UI (`pages/admin/ThreatFeedPage.tsx`) shows proper source display names ("Phishing.Database", "Phishing.Database", "AlienVault OTX", "Phishing.Database") + per-source taglines + dim icon when 0 rows; PhishTank explicitly noted as dead since 2024 in source comment.
  - **AI generation page completely rewritten.** The previous version used `text-electric-blue`, `text-foreground`, `bg-muted`, `text-muted-foreground` - none of which exist in `tailwind.config.ts`, so it rendered mostly invisible. Replaced with proper design tokens, added live correct-answer A/B/C/D distribution bar (warns yellow when any letter strays > 15 pp from 25%), Gemini + Ollama provider tiles with online/warning/offline states, per-generator cumulative breakdown, recent-12 scenarios list with `CategoryBadge` + active toggle, and an inline result panel after manual trigger.
  - Verification: frontend `tsc --noEmit` clean.
- 2026-05-02  � **Major: Rewrote LLM orchestration system** - Fixed all 3 generator bugs:
  - **Gemini**: Switched from `gemini-2.0-flash` (quota=0) to `gemini-2.5-flash` (1,500 req/day free tier)
  - **Groq**: Replaced dead Kimi/NVIDIA integration with new `groq_generator.py` using Llama 3.3 70B (14,400 req/day, 300+ tokens/sec)
  - **Ollama**: Fixed silent failures - now checks `OLLAMA_HOST` env var and logs "Ollama not available" clearly
  - Updated `llm_orchestrator.py` with new provider order: Groq → Gemini → Ollama
  - Added `GROQ_API_KEY` and `GROQ_MODEL` to `.env.example` with setup instructions
- 2026-05-02  🐛 **Hotfix: Fixed AI generation trigger endpoint** - Changed import from `generate_scenario_with_fallback` to `generate_one` and added persistence logic to save generated scenarios to database. **Note:** Flask server reload required to pick up changes.
- 2026-05-02  🐛 **Hotfix: Added missing `generator` column to Scenario model** - Required for `/admin/ai-generation/status` endpoint. Migration: `7d3f6530da5a_add_generator_column_to_scenarios.py`.
- 2026-05-02  ✅ **Bug Fixes Batch 1** - Fixed 4 critical issues:
  1. **Threat Ingestion** - Added verbose logging, lowered PhishStats score threshold (5→3), fixed OTX to accept DOMAIN/HOSTNAME indicators, fixed URLScan to accept all phishing-tagged results (removed strict malicious-only filter).
  2. **Streak Increment** - Fixed timezone issue in `_update_gamification()` by using `datetime.now(timezone.utc).date()` consistently instead of `date.today()`.
  3. **Answer Randomization** - Added `_shuffle_answers()` post-processing to all 3 LLM generators (Gemini, Kimi, Ollama) to force correct answer distribution across A/B/C/D; ran `fix_answer_patterns.py` on existing 52 manual scenarios (was 48×C, 4×B → now balanced: 15×C, 11×A, 16×B, 10×D).
  4. **AI Generation Admin Page** - New `/app/admin/ai-generation` page with status dashboard (total/by-source/by-generator counts, 24h recent), manual trigger form (count/category/difficulty), generation results display, and recent scenarios list; backend endpoints: `GET/POST /api/v1/admin/ai-generation/*`.
- 2026-05-01  ✅ **Local full-stack testing pass (pre-Phase 15)**  Backend: `pytest --cov=app` → **133 passed · 81% coverage** (target ≥75%); per-module: risk_scorer 98% · adaptive_engine 90% · kmeans 88% · random_forest 96% · security/validators 94% · auth 94% · manager 80% · admin 70%. Frontend: `tsc --noEmit` clean · `vitest run` → **11/11 passed** across 5 files (ScenarioCard, AnswerOptions, XPBar, LevelUpModal, leaderboardAnonymity) · `npm run build` → 25.9 KB CSS + 966 KB JS (gzip 276 KB). **Live-stack smoke test**: cleared sqlite DB, re-ran `python seed.py` (1 org + 5 role-typed users + 48 scenarios idempotent), booted `python wsgi.py` on :5000 + `npm run dev` on :5174. Verified end-to-end: `GET /api/v1/health` → `{status:ok, db:up, rf_model_loaded:false, kmeans_loaded:false}`; `POST /auth/login` as admin → JWT; `GET /auth/me` + `GET /admin/stats` → 200 (1 org, 5 users, 48 scenarios, job registry idle, ML models trained=false); employee (receptionist) `GET /training/session/start` → 5 scenarios (`correct_answer`/`explanation` correctly omitted), 5× `POST /session/{sid}/answer` all 200, `GET /summary` + `/scores/me` + `/gamification/me` + `/scores/leaderboard` all 200; RF model logged `"RF model not trained yet  predictor in pass-through mode"` = graceful fallback confirmed. **CORS check**: origin `http://localhost:5173` preflight returns `Access-Control-Allow-Origin: http://localhost:5173` + explicit methods (GET/POST/PATCH/DELETE/PUT/OPTIONS); origin `http://evil.example.com` returns no CORS headers → browser-level rejection = Phase 13 allowlist holds. Scheduler stayed dormant under `ENABLE_SCHEDULER=false`. Only log noise is the known `flask-limiter` in-memory warning + `datetime.utcnow()` deprecations (both tracked in THINGS_TO_FIX). **Ready to proceed to Phase 15 deployment.**
- 2026-05-01  Tracking files initialised. Read full doc index.
- 2026-05-01  `~/.gitignore` (`*`) removed so editor tools work; project `.gitignore` added.
- 2026-05-01  Phase 0.1 scaffolding files created (backend tree, frontend tree, configs, READMEs).
- 2026-05-01  Bumped requirements for Python 3.13 compatibility (pandas, numpy, sklearn, psycopg2-binary, SQLAlchemy). Documented in THINGS_TO_FIX.md.
- 2026-05-01  ✅ Phase 0.1 verification passed: `from app import create_app; create_app()` → "Scaffolding OK"; `npm install` succeeded (107 pkgs); `tsc --noEmit` clean.
- 2026-05-01  ✅ Phase 0.2 verification passed: 8 tables created (`attempts`, `organisations`, `risk_scores`, `scenarios`, `threat_feed_entries`, `user_clusters`, `user_gamification`, `users`); `User.to_dict()` confirmed never includes `password_hash`; `Scenario.applies_to_role` + XP level helpers tested; pytest 6/6.
- 2026-05-01  ✅ Phase 1 verification passed: register/login/refresh/logout/me at `/api/v1/auth/*`; JWT contains `role`+`job_role`+`org_id`; lockout after 5 failed logins → 423 with `locked_until`; logout revokes via `token_blocklist`; rate limit 10/15min on login fired correctly (saw 429 on req 11); bleach strips `<script>` from inputs; pytest 18/18 (5 reg + 4 login + 4 me/refresh/logout = 13 new auth tests).
- 2026-05-01  ✅ Phase 2 verification passed: 7-stage pipeline (`fetch → validate(HEAD) → dedupe → classify → sanitise → generate → persist`) with per-source try/except; sanitiser confirms raw URL/host **never** appears in `Scenario.content`/`visual_html`/options/explanation/title (regression test asserts both `evilhost.tk` and `paypa1-secure` substrings absent); 20-scenario-per-run cap holds (test seeded 31 unique URLs → 31 feed rows + 20 scenarios); 48h dedupe collapses trailing-slash + case duplicates; APScheduler registers 5 jobs (`threat_ingestion`/`risk_recalc`/`rf_retrain`/`kmeans_recluster`/`weekly_xp_reset`) without auto-start in dev. Bug fixed: typosquat regex matched real brand spelling  replaced with explicit `KNOWN_TYPOSQUATS` list. pytest 32/32.
- 2026-05-01  ✅ Phase 3 verification passed: `seed.py` ran cleanly  Himalayan Finance Ltd organisation (industry=finance), 5 role-typed users (admin/manager/receptionist/accountant/hr), 48 hand-written scenarios (8 categories × 2 per difficulty 1/2/3); XP rewards 10/15/25 match difficulty; visual_html present on all phishing_email + smishing entries (HTML email and SMS-bubble templates); `red_flags` valid JSON arrays of 3-5 items each; role-targeting verified (accountant→invoice fraud d2+, receptionist→tailgating, hr→fake-recruiter scenarios); seed is idempotent (re-runs skip existing titles). pytest 45/45.
- 2026-05-01  ✅ Phase 4 verification passed: `adaptive_engine` exposes `calculate_mastery` (recency-weighted with λ=0.85), `get_user_profile` (per-cat mastery + difficulty + trend + role priorities), `select_next_session` (weakest-cat 3 / other 1 / challenge 1 distribution, role-targeted, 24h-correct exclusion, prefer-unseen sort), `process_attempt` (XP + streak 1.5x@3/2.0x@7 + sentiment + risk recalc + level-up), and `get_session_summary`. `sentiment_analyzer` produces all 5 labels (rushed/overconfident/cautious/anxious/neutral) plus VADER compound on scenario content. `risk_scorer.recalculate_for_user` recomputes 8 category scores + composite + level (critical/high/medium/low/unknown). pytest 58/58 (14 new adaptive-engine tests including the 10 mandatory cases from the doc).
- 2026-05-01  ✅ Phase 5 verification passed: `RiskForestPredictor` lazy-loads pickled RandomForestClassifier from `RF_MODEL_PATH` and degrades to `predict() = None` if file is missing; `build_feature_vector_for_user` returns exactly 14 features in doc order (avg_response_time / per-cat accuracies / overall_accuracy / rushed_rate / overconfident_rate / streak_consistency / job_role_encoded / total_sessions / days_since_last / weekly_xp); job_role + risk_level encodings match doc. `train_models.py` uses `RandomForestClassifier(n_estimators=100, max_depth=8, min_samples_split=5, min_samples_leaf=2, class_weight='balanced', random_state=42)`, 80/20 stratified split, persists `risk_rf_model.pkl` + `rf_features.json` + `rf_label_encoder.pkl`, gracefully skips below `MIN_TRAINING_SAMPLES`. pytest 63/63 (5 mandatory + 1 encoding sanity).
- 2026-05-01  ✅ Phase 14 verification passed: Backend  `tests/test_risk_scorer.py` filled in with 10 cases against the actual `recalculate_for_user` implementation (no-attempts → `unknown`; all-correct → composite 0 + `low`; all-wrong → composite 100 + `critical`; mixed accuracy across two seen categories → mean 62.5 with unseen categories correctly excluded from the numerator; parametric threshold ladder 80/60/40; all 8 `{category}_score` columns populated). New `tests/test_admin.py` (17 cases) pushes admin + manager coverage way up: `/admin/stats` totals + ML flags, scenario list + toggle + create (with Phase-13 `sanitize_visual_html` asserting `<script>`/`onclick` stripped), scenario create validation (bad category → 400), users list + role change + toggle (self-demote + self-disable blocked), threats list + `trigger-feed-ingestion` (stubbed via monkeypatch, runs synchronously under `TESTING`) + `ingestion-status`, manager dashboard + team + self-profile + `/manager/history?weeks=4` + CSV reports + assign-training, invalid user id paths (400/404), `background_jobs.launch` happy + failure paths, `analyze_interaction` across rushed/overconfident/cautious inputs, legacy `PATCH /admin/scenarios/{id}`, full training session `start → answer → summary` happy path, `/scores/me/history` + `/scores/leaderboard`. `conftest.py` now exposes `admin` / `manager` / `employee` role fixtures + matching `*_headers` bearer helpers per master-doc spec (legacy tests unaffected). Verification: **`python -m pytest --cov=app`** → 133 passed, **coverage 81%** (target ≥75%, individual modules: admin 30→70%, manager 31→80%, scores 56→71%, training 64→71%, risk_scorer 98%, adaptive_engine 90%, kmeans 88%, background_jobs 43→94%). Frontend  installed `vitest@4`, `@testing-library/{react,jest-dom,user-event}`, `jsdom`, `@types/node`; wired Vitest via `vite.config.ts` (`environment=jsdom`, shared `src/test/setup.ts` polyfills `matchMedia` + `ResizeObserver` for Recharts). Five master-doc-spec Vitest files: `ScenarioCard.test.tsx` (asserts `correct_answer`/`explanation` never render even if leaked in props), `AnswerOptions.test.tsx` (3 cases: `onSelect` fires with letter, green on correct reveal, red on wrong + green on correct answer), `XPBar.test.tsx` (level + totals render, progress text correct), `LevelUpModal.test.tsx` (hidden when closed, renders level + Continue button, `onClose` on click  `react-confetti` mocked to sidestep jsdom's missing canvas context), `leaderboardAnonymity.test.ts` (regex guard: `display_name` is `First L.` only, no `risk_score`/`composite_score`/`risk_level` keys on `LeaderboardRow`). `npm test` → **11 passed**. `tsc --noEmit` clean; `npm run build` still green.
- 2026-05-01  ✅ Phase 13 verification passed: Backend  `create_app` now wires `flask_talisman.Talisman` **production-only** (`if not DEBUG and not TESTING`) with master-doc CSP (`default-src 'self'` + Google Fonts in `style-src/font-src` + `frame-src 'none'` + `frame-ancestors 'none'` + `base-uri 'self'` + `form-action 'self'`), `force_https=True`, `strict-transport-security` 1-year + subdomains, `referrer_policy=strict-origin-when-cross-origin`, `permissions_policy` camera/microphone/geolocation disabled, `X-Frame-Options: DENY`, `X-Content-Type-Options: nosniff`. CORS init hardened: `supports_credentials=False`, explicit methods + allow_headers, origins pulled from `ALLOWED_ORIGINS` env var (no wildcards allowed  asserted by test). New `sanitize_visual_html()` in `app/utils/validators.py` applies bleach allowlist (`div/p/span/table/…/button/input/label/form/style` + attrs `class/id/style` everywhere, `href` on `<a>`, `src/alt` on `<img>`) plus a second-pass linkify callback that **drops `javascript:` / `data:` / `vbscript:` hrefs**. `POST /admin/scenarios` now sanitises `visual_html` before persistence. 15 new `tests/test_security.py` cases covering every item of the master-doc 8-point security checklist: SQL-i email (401 no DB leak), XSS in registration (script stripped from returned `first_name`), tampered JWT (422 `invalid_token`), employee → manager 403, cross-org member profile 404, limiter config present + RATELIMIT_ENABLED flippable, 6× visual_html injection payloads (onclick/script/javascript:/data:/iframe/onerror), visual_html preserves safe markup, CORS no wildcards. pytest **107/107** green. Frontend  new `frontend/vercel.json` with `X-Content-Type-Options`, `X-Frame-Options: DENY`, `X-XSS-Protection`, `Referrer-Policy`, `Permissions-Policy`, `Strict-Transport-Security`. `store/authStore.ts` refactored so **access token lives in React memory only**  persist middleware points at an in-memory no-op storage and the refresh token + user shell are hand-serialised to `localStorage.ahrip_rt` + `localStorage.ahrip_user` per master-doc tradeoff. Axios timeout lowered 20s → 10s in `api/client.ts`. `tsc --noEmit` clean; `npm run build` → 26.0 KB CSS + 966 KB JS.
- 2026-05-01  ✅ Phase 12 verification passed: Backend  new `app/services/background_jobs.py` (tiny thread-safe in-process job registry, auto-sync under `TESTING`/`RUN_JOBS_SYNC`) + 9 new admin endpoints in `app/api/admin.py`: `GET /stats` (total orgs/users/scenarios + active counts + 24h threat & attempt counts + RF/KMeans trained? + last-trained timestamps from artefact mtime + current bg-job state), `POST /trigger-feed-ingestion` + `GET /ingestion-status`, `POST /retrain-models` + `GET /retrain-status` (RF uses `prepare_training_data` + `train_random_forest` respecting `MIN_TRAINING_SAMPLES`; KMeans calls `train_kmeans` + `reassign_all_users`; individual RF/KMeans failures don't kill the other), `POST /scenarios` (full validator w/ `sanitize_string` on title/content/options/explanation/tip, rejects unknown category/difficulty/role, `source=manual`, difficulty-based XP default), `PATCH /scenarios/{id}/toggle`, `PATCH /users/{id}/toggle` (org-scoped, refuses self-disable), `PATCH /users/{id}/role` (refuses self-demote). All POSTs rate-limited `30/hour`. Frontend  `api/admin.ts` (typed client for all 9 + existing endpoints, job polling helpers), four pages: `AdminDashboardPage` (4 KPI tiles with `AnimatedNumber` + two ML-model cards with green/red dot + last-trained-ago + two job cards with status badge, JSON result preview, auto-poll every 3s while `status === "running"`), `UsersPage` (search + role/status selects, inline `<select>` role changer, toggle Disable↔Re-enable, self-disable disabled by backend), `ScenariosPage` (search + category/source/difficulty filters, per-row Preview modal that re-uses the employee `ScenarioCard` + `VisualScenario` iframe for the true "what the employee sees" view + correct answer + explanation + tip in debug strip, per-row active toggle, Create-scenario modal with 4 options + answer picker + difficulty + category + explanation + tip), `ThreatFeedPage` (one card per PhishTank/Phishing.Database/OTX/URLScan with rows-ingested, scenarios-converted, last-activity ago; ingestion Trigger button + auto-poll every 3s while running; history table filterable implicitly by last 50 rows). Router grew 4 `/app/admin/*` routes gated `roles={["admin"]}`; Sidebar admin section now shows Users + Scenarios + Threat Feeds. Verification: `tsc --noEmit` clean; `npm run build` → 25.9 KB CSS + 965 KB JS (gzip 5.90 + 276 KB); backend **pytest 92/92** still green.
- 2026-05-01  ✅ Phase 11 verification passed: Backend  extended `GET /api/v1/manager/dashboard` with `kpi_cards.trend_percent` (signed accuracy Δ vs prior week) and added `GET /api/v1/manager/history?weeks=8` returning 8 weekly buckets of `{avg_accuracy, proxy_risk = 100 − accuracy·100, attempts}` for the dashboard AreaChart. Frontend  new `api/manager.ts` with typed `dashboard`/`team`/`memberProfile`/`assignTraining`/`history`/`downloadReportsCsv`, `hooks/useManagerData.ts` bulk-loads dashboard + history, `lib/archetypes.ts` mirrors backend CLUSTER_ARCHETYPES (5 entries with lucide icon + colour + intervention). Seven dashboard components: `KpiCard` (AnimatedNumber + signed-delta pill), `RiskHeatmap` (8-column CSS grid, tooltips, 15/page pagination, legend  lazy-loads per-user category scores from `/team/{id}/profile`), `RiskTrendChart` (Recharts AreaChart + ReferenceLine at 50, stroke re-coloured to risk band), `TopRiskTable` (archetype coloured badge + weakest-category icon + Assign button + "All clear" empty state), `ClusterView` (Recharts PieChart + per-archetype click-to-filter + "Action needed: X Overconfident Clickers" callout), `CategoryWeaknessChart` (sorted horizontal bars worst-first, click navigates to team-filtered), `ThreatFeedBadge` (last-update dot green<12h / amber<24h / red>24h + "PhishTank ✓ Phishing.Database ✓ AlienVault OTX ✓ URLScan ✓" source row + "updated every 6 hours" disclaimer), `AssignTrainingModal` (8-category toggle grid, note textarea, submits to `/team/{id}/assign-training`, success toast banner). Four pages: `DashboardPage` (12-col grid: 4 KPIs → Heatmap 8 / AreaChart 4 → TopRisk / ClusterPie / CategoryBars → ThreatFeed full-width), `TeamPage` (Search + Department + Risk + Archetype + Sort selectors, URL-synced `?risk=`/`?archetype=`, row expansion loads `/team/{id}/profile` and shows category bars + RF "X% susceptibility" + archetype intervention, per-row Assign button, CSV export), `ClustersPage` (5 archetype cards with count/pct/intervention and "View these staff" filter link + plain-English "how this works" note), `ReportsPage` (weeks selector + CSV download via blob). Router grew 4 `/app/manager/*` routes all gated with `ProtectedRoute roles={["manager","admin"]}`; Sidebar gained Clusters link. Verification: `tsc --noEmit` clean; `npm run build` → 25.5 KB CSS + 934 KB JS (gzip 5.82 + 270 KB  same Recharts weight, no new heavy deps); backend **pytest 92/92** still green.
- 2026-05-01  ✅ Phase 10 verification passed: Full employee app tree wired up (`/login`, `/register` + protected `/app/*` via `ProtectedRoute` + new `AppLayout` that derives `title` from the route and pipes live gamification → `streak`/`xp` into `AppShell`). `TrainingPage` ships all three master-doc states: (1) IDLE  `RiskBadge`, streak chip, hero "Today's Focus" card that reads the **weakest** category from `me.category_scores` and pulls its colour/icon/description from `lib/categories.ts`, three stat cards (scenarios this week from `/scores/me/history` last bucket, current streak, weekly XP), `DailyChallenge` card (auto-hidden when `completed`), recent earned-badges pills; (2) ACTIVE  `ScenarioCard` + `VisualScenario` iframe (sandboxed, no scripts, "Simulated Attack" warning) + `AnswerOptions` with <200ms immediate feedback + `FeedbackPanel` slide-in (explanation, red_flags, learning_tip, sentiment coach line, XP + streak bonus + badge chips); (3) COMPLETE  large `ProgressRing` (accuracy-tinted), XP summary (base / streak bonus × N / badges earned / session total), per-category mini table, risk-score delta card (captures composite on session start, diffs vs refreshed `/scores/me`), improvement tips list, dual [Start Another] / [View My Score] buttons. `LevelUpModal` fires off `useTraining.levelUpTarget` whenever any attempt returns a non-null `new_level`. `MyScorePage` renders: big `ProgressRing` (mastery = 100 − composite, colour banded by `risk_level`), `RfPredictionCard` mapping RF outputs to a plain-English "X% likely to click a real phishing link" + confidence + RiskBadge, cluster archetype card (colour-coded border, description + intervention callout), Recharts `RadarChart` across all 8 categories, 8-week Recharts `LineChart` (accuracy vs composite risk), per-category horizontal risk bars (green→yellow→orange→red banding), disclaimer. `BadgesPage` = `BadgeGrid` + All/Earned/Locked filter tabs + earned counter. `LeaderboardPage` = four-metric selector (weekly_xp / level / streak / improvement) + top-10 list with gold/silver/bronze medals for ranks 1-3, `is_current_user` row highlight + "You" pill, anonymisation disclaimer. New components: `DailyChallenge` (gold gradient + live countdown), `AppLayout` (derives title + xp from `useGamification`). `tsc --noEmit` clean; `npm run build` → 23.8 KB CSS + 860 KB JS (gzip 5.52 + 253 KB  Recharts is the weight, deferred to chunk-split in Phase 13/14). Backend suite unaffected.
- 2026-05-01  ✅ Phase 9 verification passed: Full design token set wired into `frontend/src/index.css` (`--bg-*`, `--border-*`, `--text-*`, `--risk-*`, `--accent-*`, `--xp-*`, `--rarity-*`) and mirrored in `tailwind.config.ts` as `bg-surface`, `risk-critical`, etc. Tailwind pinned to `^3.4.10` (see THINGS_TO_FIX  v4 conflicted with the v3-style `@tailwind` directives; all tokens stay version-agnostic via CSS vars). Lucide icons + Framer Motion used throughout (no `setTimeout` animation hacks). Primitives built: `ui/Button` (5 variants × 3 sizes + loading), `ui/Card` (+ Header/Title/Body/Footer), `ui/Input` (leftIcon + error/hint), `ui/Badge` (6 tones + dot), `ui/Dialog` (Esc + backdrop + scroll-lock). Shared: `RiskBadge` (pulse animation on `critical`), `AnimatedNumber` (rAF ease-out, honours `prefers-reduced-motion`), `LoadingSpinner`/`Skeleton`, `ErrorBoundary`. Training: `ProgressRing` (SVG stroke-dashoffset motion), `CategoryBadge` (8 unique icon/colour pairs from doc). Gamification: `XPBar` (bronze→gold→silver gradient, animated width), `BadgeGrid` (earned = rarity glow, unearned = grayscale + lock overlay), `StreakCounter` (flame flicker at ≥7, legendary pulse at ≥30), `LevelUpModal` (react-confetti + spring level number). Layout: `Sidebar` (4 role-gated sections + XP mini-bar footer), `TopBar` (title + streak + notifications + user dropdown), `AppShell` (responsive w/ mobile drawer via Framer Motion), `ProtectedRoute` (zustand auth store). `DesignShowcasePage` at `/design-system` exercises every primitive. `tsc --noEmit` clean; `npm run build` produces `20.4 KB CSS + 356 KB JS (gzip 4.9 + 114 KB)`. Backend suite unaffected: pytest 92/92.
- 2026-05-01  ✅ Phase 8 verification passed: All 6 API blueprints now live at `/api/v1/*`  `training` (session/start, session/{sid}/answer, session/{sid}/summary, history, categories, daily-challenge), `scores` (me, me/history, me/cluster, me/badges, leaderboard), `manager` (dashboard, team, team/{uid}/profile, team/{uid}/assign-training, reports/summary CSV), `gamification` (me, leaderboard/{metric}, badges, daily-challenge/complete), `admin` (users GET+PATCH, scenarios GET+PATCH, threats GET, threats/run-ingestion POST), `health` (db + rf_model_loaded + kmeans_loaded). Rate limits applied via `@limiter.limit`: session/start 20/hr, answer 60/hr (inherits auth 5/hr register + 10/15min login from Phase 1). Global HTTPException + Exception handlers installed in `create_app` so every error path returns JSON  verified via 404-on-unknown-route and 401-on-missing-auth tests. Session ownership enforced on POST /answer + GET /summary by checking Attempt.user_id for the session_id. Manager profile explicitly aggregates (category_scores + profile) and omits raw attempts  regression test asserts neither "attempts" key nor "answer_given" substring appears in response. Deferred: `routes/users.py` (merged into admin.py users endpoints); manager `assign-training` persistence (Phase 11 when UI consumes it  documented in THINGS_TO_FIX.md). pytest 92/92 (13 new API tests including all 9 mandatory verification items from doc + 4 invariants).
- 2026-05-01  ✅ Phase 7 verification passed: `services/gamification_engine.py` provides `XP_REWARDS` (12 reward keys from doc), `STREAK_MULTIPLIERS` = {3:1.5, 7:2.0, 14:2.5, 30:3.0}, `BADGES` (12 badges  first_blood / phishing_spotter / week_warrior / month_defender / perfect_session / phishing_master / social_shield / all_rounder / speed_reader / level_5 / level_10 / usb_aware), `RARITY_COLOURS` (common/uncommon/rare/epic/legendary). `add_xp` / `calculate_level` / `xp_to_next_level` operate on `UserGamification`; `check_and_award_badges` is hooked into `adaptive_engine.process_attempt` so badges auto-grant per attempt (session totals evaluated via live query so Flawless fires on the 5th correct answer of a session). `adaptive_engine.STREAK_BONUS_THRESHOLDS` extended to all four tiers (3/7/14/30). `get_daily_challenge` is deterministic per (user, UTC date) with `deadline` = next midnight UTC. `get_org_leaderboard` returns `{rank, display_name ("First L."), value, badge_count, level, is_current_user}` for weekly_xp / level / streak / improvement metrics  **no** risk_score or risk_level ever emitted. pytest 80/80 (10 gamification tests including all 7 mandatory cases from doc + XP threshold + badge fields + add_xp level-up).
- 2026-05-01  ✅ Phase 6 verification passed: 5 `CLUSTER_ARCHETYPES` with full `label`/`description`/`colour`/`icon`/`intervention` fields matching master doc (Overconfident Clicker #EF4444 zap, Cautious Learner #22C55E shield-check, Inconsistent Performer #F59E0B alert-triangle, Resilient Defender #3B82F6 award, Disengaged Completer #8B5CF6 eye-off); 6-feature vector (avg_response_time_ms / overall_accuracy / accuracy_variance / rushed_rate / total_sessions / streak_consistency)  **not 14**, per doc; `train_kmeans` fits `StandardScaler` then `KMeans(n_clusters=5, random_state=42, n_init=10)` and persists both in a **single joblib bundle** at `KMEANS_MODEL_PATH` so inference uses identical scaling; `assign_user_to_cluster` writes `User.cluster_label` + `User.cluster_assigned_at` **and** appends a `UserCluster` history row with the feature vector as JSON; `get_org_cluster_summary` returns `{archetypes[5], most_common_archetype, highest_risk_archetype_count, intervention_required[]}`; graceful fallback  missing bundle → `UserClusterer.is_ready=False`, `predict_cluster`/`assign_user_to_cluster` both return `None`; training refuses to run below 5 eligible users; scheduler `kmeans_recluster` job (interval=7d) now calls `train_kmeans` + `reassign_all_users`; `train_models.py` drives both RF and KMeans when `--rf-only` is not passed. pytest 71/71 (8 new KMeans tests).
